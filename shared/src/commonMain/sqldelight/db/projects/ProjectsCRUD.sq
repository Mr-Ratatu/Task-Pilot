getAllProject:
SELECT *
FROM projectDTO;

insertProject:
INSERT INTO projectDTO(id, name, desciption, boardId)
VALUES (?, ?, ?, ?);

deleteProejct:
DELETE FROM projectDTO WHERE id IS :id;

updateProject:
UPDATE projectDTO SET
name = :name,
desciption = :desciption,
boardId = :boardId
WHERE id IS :id;